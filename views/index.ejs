<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="format-detection" content="telephone=no">
    <title>Dashboard | LoadXpress</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../css/main.css" />
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
</head>
<body>

    <div class="sidebar-overlay" id="overlay" onclick="toggleSidebar()"></div>
    <div id="toast">Copied to clipboard!</div>

    <div class="app-layout">
        <aside class="sidebar" id="sidebar">
            <div>
                <div class="brand">
                    <img src="/assets/logo.png" alt="LoadXpress Logo" style="width: 35px; height: 35px; object-fit: contain;" />
                    <span>LoadXpress</span>
                </div>
                <nav class="side-nav">
                    <a href="#" class="nav-item active">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect x="14" y="14" width="7" height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg>
                        Dashboard
                    </a>

                    <a href="/me/order" class="nav-item">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="1" x2="12" y2="23"></line><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path></svg>
                        Purchase
                    </a>
                    <a href="/me/transaction-history" class="nav-item">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line><polyline points="10 9 9 9 8 9"></polyline></svg>
                        Transactions
                    </a>


                    <a href="/me/withdraw" class="nav-item">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22v-8m0-8V2"/><path d="M20 10c0-4.4-3.6-8-8-8s-8 3.6-8 8 3.6 8 8 8h.5"/><path d="M4 14l2 2 2-2"/><path d="M16 14l2 2 2-2"/></svg>
                        Withdraw
                    </a>


                    
                    <a href="#" class="nav-item">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <polyline points="16 18 22 12 16 6"></polyline>
                            <polyline points="8 6 2 12 8 18"></polyline>
                        </svg>
                        APIs
                    </a>


                    
                    <a href="/me/settings" class="nav-item">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>
                        Settings
                    </a>
                </nav>
            </div>
        </aside>

        <main class="main-view">
            <header class="top-bar">
                <div style="display: flex; align-items: center; gap: 15px;">
                    <div class="hamburger" onclick="toggleSidebar()">
                        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#1e293b" stroke-width="2.5"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
                    </div>
                    <div class="page-title">
                        <p id="greeting">Welcome back,</p>
                        <h1><%= user.first_name || "Rookie"%>!</h1>
                    </div>
                </div>

                <div class="profile-container">
                    <img src="<%=user.picture%>" alt="Profile" class="profile-trigger" onclick="toggleProfileMenu()">
                    <div class="profile-menu" id="profileMenu">
                        <a href="/me/settings" class="menu-link">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>
                            Settings
                        </a>
                        <a href="/logout" class="menu-link logout">
                            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
                            Logout
                        </a>
                    </div>
                </div>
            </header>

            <div class="dashboard-grid">
                <div class="wallet-card">
                    <div class="card-top">
                        <div class="chip-icon"></div>
                        <span class="card-label">Personal Account</span>
                    </div>

                    <div class="balance-section">
                        <span style="font-size: 12px; opacity: 0.8">Total Balance</span>
                        <h2>₦ <%= user.balance.toLocaleString(undefined, {                           minimumFractionDigits: 2, 
maximumFractionDigits: 2
}) %></h2>
                        <span class="growth-badge">+12%</span>
                    </div>

                    <div class="card-bottom">
                        <div class="account-info" onclick="copyText('8029334112')">
                            <span class="bank-name">Wema Bank</span>
                            <div class="acc-digits">
                                8029 334 112 
                                <svg id="copy-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
                            </div>
                        </div>
                        <a class="w-btn" href="/me/withdraw" style="text-decoration: none;">Transfer</a>
                    </div>
                </div>

                <div class="services-grid">
                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/airtime.jpeg" width="35">
                        </div>   
                        <h4>Airtime</h4>
                    </a>
                    <a class="service-box" href="/me/order">

                        <div class="sb-icon">
                           <img src="/assets/data.jpeg" width="35">
                        </div>   
                        
                        <h4>Data</h4>
                    </a>
                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/bills.jpeg" width="35">
                        </div>
                        <h4>Bills</h4>
                    </a>
                    <a class="service-box" href="/me/order">
                        
                        <div class="sb-icon">
                           <img src="/assets/refer.webp" width="35">
                        </div>
                        <h4>Referral</h4>
                    </a>

                    
                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/electricity.jpeg" width="35">
                        </div>   <h4>Electricity</h4>
                    </a>

                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/gotv.png" width="35">
                        </div>   <h4>GOTV</h4>
                    </a>

                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/dstv.webp" width="35">
                        </div>   <h4>DSTV</h4>
                    </a>


                    <a class="service-box" href="/me/order">
                        <div class="sb-icon">
                           <img src="/assets/startimes.webp" width="35">
                        </div>   <h4>Startimes</h4>
                    </a>

                </div>

                <div class="chart-card">
                    <div class="chart-header">
                        <h3>Weekly Spending Overview</h3>
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#64748b" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line></svg>
                    </div>
                    <div class="chart-container">
                        <canvas id="spendingChart"></canvas>
                    </div>
                </div>
            </div>

            <section class="tx-section">
                <div class="section-head">
                    <h3>Recent Transactions <span style="display: inline-block; width: 5px; height: 5px; background-color: lightgreen; border-radius: 50%; vertical-align: middle; margin-left: 5px;"></span></h3>
                    <a href="/me/transaction-history" style="color: var(--accent); font-weight: 700; font-size: 13px; text-decoration: none;">View All</a>
                </div>

                <div id="loading-history" style="text-align: center; margin-block: 40px 30px;">
                    Loading transactions...
                </div>

                <div id="empty-history" style="
                    width: 100%;
                    height: 200px;
                    margin-block: 30px 20px;
                    display: none;
                    flex-direction: column;
                    justify-content: center;
                    align-items: center;
                    gap: 10px;
                    ">
                    <img src="assets/empty.svg">
                    <p>
                        No transaction found.
                    </p>
                </div>
                
            </section>
        </main>
    </div>


    <div class="fab-container" id="fab">
        <div class="fab-menu">
            <div class="fab-item" onclick="window.location.reload()">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20">
                    <path fill="#1e293b" d="M15.65 4.35A8 8 0 1 0 17.4 13h-2.22a6 6 0 1 1-1-7.22L11 9h7V2z" />
                </svg>
                
            </div>
            <div class="fab-item" onclick="window.location.href = '/me/order';"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2.69l5.66 5.66a8 8 0 1 1-11.31 0z"></path></svg>
            
            </div>
            <div class="fab-item" onclick="window.location.href = '/me/settings';"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg></div>
        </div>
        <div class="fab-main" id="fabTrigger">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="../assets/js/main.js"></script>
    
    <script>
        const greetingEl = document.getElementById('greeting');
          const hour = new Date().getHours();
          let greeting = 'Welcome back';

          if (hour >= 5 && hour < 12) {
            greeting = 'Good morning';
          } else if (hour >= 12 && hour < 18) {
            greeting = 'Good afternoon';
          } else if (hour >= 18 && hour < 22) {
            greeting = 'Good evening';
          } else {
            greeting = 'Good night';
          }

          greetingEl.textContent = `${greeting},`;

        const alertIt = (icon, title) => {
            Swal.fire({
                toast: true,
                position: "top-end",
                title,
                icon,
                timer: 5000,
                timerProgressBar: true,
                showConfirmButton: false,
            });
        };
 
        // 3. COPY FUNCTIONALITY
        function copyText(text) {
            navigator.clipboard.writeText(text).then(() => {
                const toast = document.getElementById("toast");
                toast.className = "show";
                setTimeout(() => { toast.className = toast.className.replace("show", ""); }, 3000);
            });
        };


        function renderTransactions(transactions){

            const txSection = document.querySelector(".tx-section");
           document.getElementById("loading-history").style.display = 'none';
            
            if (!transactions || transactions.length === 0){
                return document.getElementById("empty-history").style.display = "flex";
            };

            const helper = {
                vtu: (t) => { return `<img src="/assets/${t}.png" class="tx-img"/>`},
                fund: (t) => { return t === "debit" ? `
<div style="color: #dc2626; background: #fee2e2;" class="tx-img">
<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 19V5M5 12l7-7 7 7"/></svg>
</div>
` : `<div style="color: #16a34a; background: #dcfce7;" class="tx-img">
<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 5v14M19 12l-7 7-7-7"/></svg>
</div>
`},
                none: (t) => { return null}
            };


            for (const transaction of transactions){
                try {
                    //alert(JSON.stringify(transaction));
                const { amount, type, sub, subInfo, debit, which } = transaction;
                const element = document.createElement("div");
                element.className = "tx-row";
                const im = helper[type || "none"](which);
                
                element.innerHTML = `
    <div style="display:flex; align-items:center">
    ${im}
        <div class="tx-info">
            <h5>${sub}</h5>
            <p>${subInfo}</p>
        </div>
    </div>
    <div class="tx-amount ${debit === true ? '' : 'pos'}">${debit === true ? '' : '+'}₦ ${amount.toLocaleString(undefined, {
        minimumFractionDigits: 2, 
        maximumFractionDigits: 2
    })}</div>
`;
                txSection.appendChild(element);

                } catch (e){
                    alert(e.message);
                    };
            };
            
        };


    async function renderChartAndLastFiveTransactions() {
        let labels;
        let data;
        let tss;
        try {
            const r = await fetch('/api/get-transactions');
            if (!r.ok){
                alertIt("error","Site error while fetching transactions. Please try again later!");
                throw new Error();
            };
            const res = await r.json();
            if (res.success !== true){
                alertIt("error", res.error || "Site error while fetching transactions. Please try again later!");
                throw new Error();
            };
            labels = res.details.labels;
            data = res.details.data;
            tss = res.details.transactions;
        } catch (e){
            labels = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
            data = [0, 0, 0, 0, 0, 0, 0];
        };
        renderTransactions(tss);
        // 4. CHART CONFIG 
        const ctx = document.getElementById('spendingChart').getContext('2d');
        let gradient = ctx.createLinearGradient(0, 0, 0, 300);
        gradient.addColorStop(0, 'rgba(99, 102, 241, 0.4)');
        gradient.addColorStop(1, 'rgba(99, 102, 241, 0.0)');

        new Chart(ctx, {
            type: 'line',
            data: {
                labels,
                datasets: [{
                    label: 'Transact',
                    data,
                    borderColor: '#6366f1',
                    backgroundColor: gradient,
                    borderWidth: 2,
                    tension: 0.4,
                    fill: true,
                    pointRadius: 3,
                    pointHoverRadius: 4
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: { legend: { display: false } },
                scales: {
                    x: { grid: { display: false }, ticks: { color: '#94a3b8', font: {size:11} } },
                    y: { display: false}
                }
            }
        });

    };

        renderChartAndLastFiveTransactions();
        

        // 4. IMPROVED DRAGGABLE FAB
        const fab = document.getElementById('fab');
        const trigger = document.getElementById('fabTrigger');
        let isDragging = false, startX, startY, initialLeft, initialTop;

        // Mouse/Touch Handlers
        trigger.addEventListener('mousedown', dragStart);
        trigger.addEventListener('touchstart', dragStart, {passive: false});

        function dragStart(e) {
            const clientX = e.clientX || e.touches[0].clientX;
            const clientY = e.clientY || e.touches[0].clientY;
            startX = clientX; startY = clientY;
            const rect = fab.getBoundingClientRect();
            initialLeft = rect.left; initialTop = rect.top;

            document.addEventListener('mousemove', dragMove);
            document.addEventListener('touchmove', dragMove, {passive: false});
            document.addEventListener('mouseup', dragEnd);
            document.addEventListener('touchend', dragEnd);
        }

        function dragMove(e) {
            const clientX = e.clientX || e.touches[0].clientX;
            const clientY = e.clientY || e.touches[0].clientY;

            if (Math.abs(clientX - startX) > 5 || Math.abs(clientY - startY) > 5) {
                isDragging = true;
                e.preventDefault();
                fab.classList.remove('active'); // Close menu while dragging

                let newLeft = initialLeft + (clientX - startX);
                let newTop = initialTop + (clientY - startY);

                // Boundaries
                const maxLeft = window.innerWidth - 60;
                const maxTop = window.innerHeight - 60;
                newLeft = Math.max(10, Math.min(newLeft, maxLeft));
                newTop = Math.max(10, Math.min(newTop, maxTop));

                fab.style.left = `${newLeft}px`;
                fab.style.top = `${newTop}px`;
                fab.style.bottom = 'auto'; fab.style.right = 'auto';
            }
        }

        function dragEnd() {
            document.removeEventListener('mousemove', dragMove);
            document.removeEventListener('touchmove', dragMove);
            document.removeEventListener('mouseup', dragEnd);
            document.removeEventListener('touchend', dragEnd);

            if (isDragging) {
                // Snap to nearest vertical edge
                const rect = fab.getBoundingClientRect();
                const screenWidth = window.innerWidth;
                const snapThreshold = screenWidth / 2;

                if (rect.left < snapThreshold) {
                    fab.style.left = '20px'; // Snap Left
                } else {
                    fab.style.left = `${screenWidth - 76}px`; // Snap Right
                }
                setTimeout(() => isDragging = false, 100);
            }
        }

        trigger.addEventListener('click', (e) => {
            if (!isDragging) fab.classList.toggle('active');
        });

    </script>
</body>
</html>
